package com.air.platform.common.agent;

public abstract class VideoGenerationAgent extends DualChannelAgent {
    protected VideoGenerationAgent(Builder<?, ?> builder) {
        super(builder);
    }
}