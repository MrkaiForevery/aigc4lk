package com.air.platform.common.agent;

public abstract class ImageGenerationAgent extends DualChannelAgent {
    protected ImageGenerationAgent(Builder<?, ?> builder) {
        super(builder);
    }
}